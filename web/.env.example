# Database
# Use absolute path so Prisma finds the SQLite DB regardless of working directory.
DATABASE_URL="file:~/PainPoints/web/dev.db"

# Service Ports
LM_STUDIO_PORT="1234"
NEXTJS_PORT="3000"

# LM Studio Settings
LM_STUDIO_DIR="$HOME/Downloads"
# If empty, the script auto-detects the latest LM Studio AppImage in LM_STUDIO_DIR
LM_STUDIO_APPIMAGE_NAME=""
MODEL_CONTEXT_LENGTH="25000"

# LM Studio Configuration (used by the Next.js app)
LLM_BASE_URL="http://localhost:1234"
LLM_MODEL="" # If blank, start.sh auto-detects from `lms ls` and updates this file
LLM_REQUEST_TIMEOUT_MS="180000"

# Reddit API
# No credentials needed! The app uses Reddit's public JSON API for read-only access.
# Rate limit: 60 requests/minute (handled automatically by the app)

# Next.js Mode
# Set to "prod" for production build or "dev" for development mode (default)
NEXTJS_MODE="dev"
